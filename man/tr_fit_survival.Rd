% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-fit-survival.R
\name{tr_fit_survival}
\alias{tr_fit_survival}
\title{Fits a survival model using the \code{censored}/\code{parsnip} framework. The
outcome must be specified as separate time and event columns (following
\code{survival::Surv()} convention), and the user must explicitly choose an
engine.}
\usage{
tr_fit_survival(
  data,
  time_col,
  event_col,
  engine = c("cox_ph", "survival_rf"),
  predictors = NULL
)
}
\arguments{
\item{data}{A data frame containing predictors, a time column, and an
event column.}

\item{time_col}{Character. Name of the column giving time to event or
censoring.}

\item{event_col}{Character. Name of the column indicating event status
(1 = event occurred, 0 = censored), following standard survival
analysis convention.}

\item{engine}{Character. One of \code{"cox_ph"} (Cox Proportional Hazards) or
\code{"survival_rf"} (Random Survival Forest via the \code{aorsf} engine).}

\item{predictors}{Character vector of predictor column names. If \code{NULL}
(default), all columns except \code{time_col} and \code{event_col} are used.}
}
\value{
A fitted \code{triageR_survival_model} object (list) containing the
fitted workflow, engine, and time/event/predictor column names.
}
\description{
Fits a survival model using the \code{censored}/\code{parsnip} framework. The
outcome must be specified as separate time and event columns (following
\code{survival::Surv()} convention), and the user must explicitly choose an
engine.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  data(lung, package = "survival")
  lung_clean <- lung
  lung_clean$status <- lung_clean$status - 1  # convert 1/2 to 0/1
  lung_clean <- lung_clean[stats::complete.cases(lung_clean), ]
  model <- tr_fit_survival(lung_clean, time_col = "time",
                            event_col = "status", engine = "cox_ph")
}
}
