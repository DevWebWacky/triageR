% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-validate-survival.R
\name{tr_validate_survival}
\alias{tr_validate_survival}
\title{Validate a clinical survival model}
\usage{
tr_validate_survival(model, newdata = NULL)
}
\arguments{
\item{model}{A fitted \code{triageR_survival_model} object from
\code{tr_fit_survival()}.}

\item{newdata}{Optional data frame to validate on. If \code{NULL} (default),
validation runs on the original training data, with a warning.}
}
\value{
A \code{triageR_survival_validation} object (list) containing the
C-index and supporting details.
}
\description{
Computes the concordance index (C-index) for a fitted
\code{triageR_survival_model} — the survival-analysis equivalent of AUC,
measuring how well the model ranks patients by risk. Can validate on
new (external/holdout) data, or fall back to the training data with a
clear warning.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  library(survival)
  lung_clean <- lung
  lung_clean$status <- lung_clean$status - 1
  lung_clean <- lung_clean[stats::complete.cases(lung_clean), ]
  model <- tr_fit_survival(lung_clean, time_col = "time",
                            event_col = "status", engine = "cox_ph")
  tr_validate_survival(model, newdata = lung_clean)
}
}
