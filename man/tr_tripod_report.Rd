% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-tripod.R
\name{tr_tripod_report}
\alias{tr_tripod_report}
\title{Generate a TRIPOD+AI-aligned clinical model report}
\usage{
tr_tripod_report(
  model,
  validation = NULL,
  review = NULL,
  sensitivity = NULL,
  recommendation = NULL,
  output_file = file.path(tempdir(), "triageR_report"),
  format = c("html", "docx")
)
}
\arguments{
\item{model}{A fitted \code{triageR_model} object from \code{tr_fit()}.}

\item{validation}{Optional \code{triageR_validation} object from \code{tr_validate()}.}

\item{review}{Optional \code{triageR_review} object from \code{tr_agent_review()}.}

\item{sensitivity}{Optional \code{triageR_sensitivity} object from \code{tr_sensitivity()}.}

\item{recommendation}{Optional character string from \code{tr_recommend_method()}.}

\item{output_file}{Character. File path (without extension) to save the
report to. Defaults to a file in \code{tempdir()}. Set explicitly (e.g.
\code{file.path("my_folder", "report")}) to save elsewhere.}

\item{format}{Character. Either \code{"html"} (default) or \code{"docx"}.}
}
\value{
Invisibly returns the path to the rendered report file.
}
\description{
Renders a reproducible report summarizing model fit, validation,
sensitivity analysis, and pipeline review, aligned with TRIPOD+AI
reporting guidance. This is a drafting aid, not a certified compliance tool.
}
\examples{
\dontrun{
tr_tripod_report(model, validation = val, review = rev,
  output_file = file.path(tempdir(), "report"))
}
}
